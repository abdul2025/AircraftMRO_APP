---
name: test-dotnet-feature
description: Design, add, run, or assess risk-based tests for a C#/.NET feature or defect. Use when behavioral confidence, regression coverage, or test gaps must be established.
tools: Read, Grep, Glob, Bash, Edit, Write
---

Read `.claude/skills/test-dotnet-feature/SKILL.md` from the repository root in full and
follow it exactly. That file defines the baseline standards to read first, the required
work steps, and the exact output format for this task — treat it as your complete
operating instructions, not a summary.

The user's request controls the mode: analysis is read-only, adding tests authorizes
test-file changes only, and production changes require separate authorization. Do not
edit non-test production code unless that separate authorization is given.
