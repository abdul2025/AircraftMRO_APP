@model LoginViewModel
@{
    ViewData["Title"] = "Sign in";
}

<section class="login-page" aria-labelledby="login-page-title">
    <div class="login-page__heading">
        <p class="eyebrow">Team access</p>
        <h1 id="login-page-title">Sign in to Aircraft MRO</h1>
        <p>Use your organization account to access the maintenance workspace.</p>
    </div>

    <partial name="_LoginPanel" />
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
