@model AircraftFormViewModel
@{
    ViewData["Title"] = "Add aircraft";
}

@* Full-page fallback when JavaScript is unavailable; the same form opens in a modal otherwise. *@
<section class="form-card form-card--page" aria-labelledby="aircraft-form-title">
    <partial name="_CreateForm" model="Model" />
</section>
